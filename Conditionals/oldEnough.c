#include <stdio.h>
#include <stdbool

int age = 20;
it main (void){
    printf("how old are you?")
    if(age >= 18){
        printf("you are old enough to vote!");
    }else if (age =< 18){
        printf("you are not old enought to vote");
    };
    return 0:
}